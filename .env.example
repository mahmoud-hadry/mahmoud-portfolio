# Example environment file
# Copy to `.env` for local overrides if needed.
# Never commit real tokens or secrets.

# Vite exposes only variables prefixed with VITE_
# VITE_SITE_URL=https://mahmoud-hadry.github.io/
